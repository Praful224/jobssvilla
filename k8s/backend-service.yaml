apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: jobsvilla-prod
  labels:
    app: jobsvilla-backend
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: jobsvilla-backend
  type: ClusterIP
