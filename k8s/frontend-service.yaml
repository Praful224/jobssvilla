apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: jobsvilla-prod
  labels:
    app: jobsvilla-frontend
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: jobsvilla-frontend
  type: LoadBalancer
